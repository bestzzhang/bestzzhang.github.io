# Single-page mode: top navigation intentionally left empty so the masthead
# only shows the site title (matches the reference layout).
#
# To add nav items back, uncomment entries here, e.g.:
# main:
#   - title: "CV"
#     url: /cv/

main: []
